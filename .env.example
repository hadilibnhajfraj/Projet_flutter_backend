# ================== API CONFIG ==================
PORT=4000

# ================== DATABASE ===================
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123456
DB_NAME=cbitunisiaGestionnaire
DATABASE_URL=postgres://postgres:123456@127.0.0.1:5432/cbitunisiaGestionnaire

# ============== JWT KEYS & TOKENS ==============
JWT_ACCESS_SECRET=super_long_access_secret_change_me_123456789
JWT_REFRESH_SECRET=super_long_refresh_secret_change_me_123456789
ACCESS_TOKEN_EXPIRES_IN=7d
REFRESH_TOKEN_EXPIRES_IN=7d



# ============= reCAPTCHA (DEV) ================
# En dev tu peux laisser RECAPTCHA_SECRET vide,
# le backend loggue "[reCAPTCHA] No SECRET configured. Skipping verification (DEV ONLY)."
RECAPTCHA_SECRET=

# ============= SMTP / EMAIL (GMAIL) ============
##EMAIL_HOST=smtp.gmail.com
# ============= SMTP / EMAIL (HOSTINGER) ============
EMAIL_HOST=smtp.hostinger.com
EMAIL_PORT=587
#EMAIL_USER=t0429597@gmail.com
#EMAIL_PASS=vbyspvizzdwnqppu
#EMAIL_SECURE=false
EMAIL_USER=cbitunisia@cbi-tunisia.com
EMAIL_PASS=Cbi2026@Secure!Mail
EMAIL_SECURE=false
EMAIL_FROM="CBI Tunisia <cbitunisia@cbi-tunisia.com>"

# ============= AUTOMATIC EMAILS (CRON JOBS) ============
# Master switch for ALL automatic/scheduled email cron jobs (relance, follow-up, etc.).
# Read by: src/services/scheduler.js, src/cron/checkProjects.js, src/cron/projectCron.js,
# src/cron/followup.job.js, src/services/reminderService.js.
# Default: DISABLED. Set to "true" only when you want the CRM to resume sending automatic emails.
AUTO_EMAIL_JOBS_ENABLED=true

# Verrouillage automatique des fiches PROBAR/PROMESH 24h après création
# (voir src/cron/ficheAutoValidation.job.js). Actif par défaut (contrairement
# aux flags ci-dessus) — mettre à "false" pour désactiver sans redéploiement.
AUTO_VALIDATION_ENABLED=true
# Ne s'applique jamais aux fiches créées avant cette date (ISO) — protège les
# fiches déjà en base au moment de l'introduction de cette règle. Ajuster
# uniquement si vous voulez décaler le point de bascule.
AUTO_VALIDATION_CUTOFF_AT=2026-08-11T00:00:00Z
SUPERADMIN_EMAIL=cbitunisia@cbi-tunisia.com
SUPERADMIN_PASSWORD=221SFT0104h*
SUPERADMIN_1_EMAIL=bayrem@gmail.com
SUPERADMIN_1_PASSWORD=221SFT0104h.


# ── Google Calendar OAuth2 (voir GOOGLE_CALENDAR_SETUP.md) ─────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/google-calendar/callback
GOOGLE_TOKEN_ENC_KEY=30c6a4cb930d149cd89d087476106f23730fc5608dba507fcad1adc236766522
# URL HTTPS PUBLIQUE du backend, utilisée pour enregistrer le canal push
# Google Calendar (events.watch) — sync entrante Google -> CRM. Laisser vide
# en dev local (localhost n'est pas joignable par Google) : le système
# continue de fonctionner en écriture CRM -> Google uniquement. Exemple prod :
# GOOGLE_CALENDAR_WEBHOOK_BASE_URL=https://api.crmprobar.com
GOOGLE_CALENDAR_WEBHOOK_BASE_URL=

# ── Demandes d'archivage/désarchivage — lien CRM inclus dans les emails ────
APP_URL=http://localhost:3000

# ── WhatsApp (Twilio) — notifications des demandes d'archivage ────────────
# Tant que les 3 variables TWILIO_* ne sont pas renseignées, l'envoi WhatsApp
# est silencieusement ignoré (src/services/whatsapp.service.js ne lève jamais
# d'exception). Renseignez-les pour activer l'envoi réel.
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_NUMBER=
# Numéro WhatsApp de cbitunisia@cbi-tunisia.com qui recevra les notifications
# de nouvelles demandes (format E.164, ex: +21612345678).
# CBI_ADMIN_WHATSAPP_NUMBER=

JWT_MFA_SECRET=c4f9594b8621715087769fc0b88e7635d485bb4c9ff380ac9b52aa0d966d592ef8c16712909173e1bb972723911a9138

# MFA — voir src/config/mfaConfig.js. true = comportement actuel (OTP email), false = bypass complet (login classique email+mdp). Repasser a true pour reactiver le MFA sans changement de code.
MFA_ENABLED=false
